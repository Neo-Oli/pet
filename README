Virtual Pet simulator written in python. Actions are performed with arguments. Programm will only run for one action, but will save it's state.

After initializing once, you can edit the config file under ~/.config/pet/
